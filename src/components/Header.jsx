import React from 'react';
import '../css/header.custom.css';

const Header = () => {
    return (
        <header>
            <h1> Find yourself a movie </h1>
        </header>
    );
};

export default Header;